VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_ANON_KEY=
# Web Push (desktop). Generate with: npx web-push generate-vapid-keys --json
# Set VAPID_PUBLIC_KEY + VAPID_PRIVATE_KEY + VAPID_EMAIL as Supabase secrets too.
VITE_VAPID_PUBLIC_KEY=
